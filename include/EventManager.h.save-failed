#ifndef EVENTMANAGER_H
#define EVENTMANAGER_H
#include <map>
#include "EventArgs.h"


class EventManager
{
    private:
        std::map< std::string, EventHandler*> EventMapper;
        static EventManager* current_manager;

    public:
        EventManager();
        virtual ~EventManager();
        static EventManager* Instance();
    protected:
};

#endif // EVENTMANAGER_H
